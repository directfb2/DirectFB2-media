DirectFB2-media is built using Meson.

First, configure the build:

  $ meson setup build/

Start the build with:

  $ meson compile -C build/

And install DirectFB2-media using:

  $ meson install -C build/
