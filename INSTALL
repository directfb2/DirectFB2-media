Installation Instructions
=========================

Linux
=====

First, configure the build:

  $ meson setup build/

Start the build with:

  $ meson compile -C build/

And install DirectFB2-media using:

  $ meson install -C build/

NuttX
=====

On NuttX, copy the DirectFB2-media directory to your NuttX apps/graphics/ directory.
